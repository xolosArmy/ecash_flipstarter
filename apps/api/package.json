{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "lint": "eslint . --ext .ts",
    "test": "echo \"No tests defined for api service\"",
    "typecheck": "tsc --noEmit",
    "format": "prettier --check ."
  },
  "dependencies": {
    "@ecash-flipstarter/config": "workspace:*",
    "@ecash-flipstarter/sdk": "workspace:*",
    "@ecash-flipstarter/types": "workspace:*",
    "axios": "^1.6.7",
    "dotenv": "^16.3.1",
    "fastify": "^4.25.0",
    "fastify-plugin": "^4.5.0",
    "pino": "^8.16.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
